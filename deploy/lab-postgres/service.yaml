apiVersion: v1
kind: Service
metadata:
  name: brickline-postgres
  namespace: brickline
  labels:
    app.kubernetes.io/name: brickline-postgres
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: brickline-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres

